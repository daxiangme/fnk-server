package ${package.Parent}.biz.facade;

import ${package.Parent}.api.facade.${entity?replace("DO", "")}Facade;
import org.springframework.stereotype.Service;

/**
* ${table.comment!} facade 实现。
*
* @author ${author}
*/
@Service
public class ${entity?replace("DO", "")}FacadeImpl implements ${entity?replace("DO", "")}Facade {

}
