package ${package.Parent}.biz.convert;

/**
* ${table.comment!} 对象转换。
*
* @author ${author}
*/
public class ${entity?replace("DO", "")}Convert {
    private ${entity?replace("DO", "")}Convert() {
    }
}
